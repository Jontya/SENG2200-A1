
// The interface implemented inside of the Polygon class to compare two polygons and determine if this.Polygon should come before the param
public interface ComparePoly { 
    boolean comesBefore(Polygon o);
}